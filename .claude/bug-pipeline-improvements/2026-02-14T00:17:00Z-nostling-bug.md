# Bug Fix Pipeline Improvement

**Run**: 2026-02-14T00:17:00Z
**Project**: nostling
**Pipeline Outcome**: REVISION_REQUIRED (missing CHANGELOG.md documentation)
**Remediation Attempts**: 1 of 3
**Commit**: f490cb3

## Bug Context

**Bug**: unread-badge-not-clearing
**Symptom**: Unread message badge persists after opening conversation; messages remain marked as unread even while user is viewing the conversation
**Root Cause**: `markMessagesRead` only fires on contact selection change via `onSelectContact` callback, not when new messages arrive in already-selected conversation
**Fix Applied**: Added `useEffect` hook (main.tsx:2197-2206) that monitors `unreadCounts` and auto-calls `markMessagesRead` when count > 0 for currently-selected contact

## What Went Wrong

Initial fix implementation was technically correct and complete but missing CHANGELOG.md documentation specified in contract's `expected_collateral`. Verification returned REVISION_REQUIRED, requiring remediation round to add missing documentation.

## Information Gaps

### 1. Missing CHANGELOG.md Update

**Gap**: Contract's `expected_collateral` section specified CHANGELOG.md update (lines 56-58) but implementer did not add it during Phase 3 fix implementation

**When Needed**: FIX_IMPLEMENTATION

**Impact**: WOULD_HAVE_PREVENTED remediation round. Implementer would have added CHANGELOG.md entry as part of fix commit rather than requiring separate remediation. Documentation phase would have been complete on first attempt.

**Where It Existed**: Fix contract JSON file (bug-reports/unread-badge-not-clearing-contract.json lines 54-62), identical pattern from previous bug fix (footer-missing-relay-status had same expected_collateral structure and completed successfully with CHANGELOG entry)

**Search Query**: `expected_collateral documentation CHANGELOG required`

**Confidence**: 0.95

### 2. Pre-existing Temporary Files Not Cleaned

**Gap**: `.claude/bug-pipeline-improvements/` directory from previous bug fix (footer-missing-relay-status, Feb 14 18:56) persisted in workspace, violating CLAUDE.md cleanup directive (line 2)

**When Needed**: PRE-VALIDATION (before starting unread-badge fix)

**Impact**: WOULD_HAVE_REDUCED verification noise. Temporary files from footer fix would have been removed before starting unread-badge fix. Examiner would not have flagged this issue (BVQ-007, severity 5), reducing noise in verification results.

**Where It Existed**: CLAUDE.md project instructions (line 2: "Cleanup temporary files after implementation"), .gitignore patterns for temporary files, workspace status check

**Search Query**: `temporary files cleanup CLAUDE.md directive workspace hygiene`

**Confidence**: 0.88

## Verification Findings

| Question | Result | Severity | Key Evidence |
|----------|--------|----------|--------------|
| Has the root cause been addressed, not just symptoms? | YES | 10 | useEffect directly addresses missing auto-mark mechanism identified in root cause analysis |
| Does a regression test exist that would catch this bug if reintroduced? | YES | 9 | 4 comprehensive tests cover all edge cases and would catch reintroduction |
| Does the implementation contain changes not documented in the fix contract? | PASS | 9 | Only main.tsx modified (15 lines), matches contract exactly |
| Are there similar patterns in the codebase that might have the same bug? | NO | 6 | Contract confirms "only location where unread badge clearing is handled" |
| Is there any dead code? | PARTIAL | 3 | No production dead code; pre-existing temp files from previous fix |
| Are there any parallel implementations? | PASS | 1 | Two complementary triggers (onSelectContact, useEffect), same backend |
| Are all obsolete files cleaned up? | NO → REMEDIATED | 5 | Pre-existing temp files from footer fix, cleaned during remediation |
| Does the fix handle both edge cases? | YES | 9 | Both edge cases (manual selection, auto-mark in open conversation) handled |

**Critical Findings** (severity >=7 or NO/PARTIAL):
- Missing CHANGELOG.md documentation (contract expected_collateral violation) - remediated in attempt 1
- Pre-existing temporary files from previous bug fix - cleaned during remediation

## Pipeline Improvements

### 1. FIX_IMPLEMENTATION: Add expected_collateral checklist

**Trigger**: When implementing fix in Phase 3, before marking STATUS: FIX_APPLIED
**Action**: Read contract's `expected_collateral` section and create checklist of all required deliverables (regression_test, documentation, similar_patterns_fixed). Verify each item before completing phase.
**Query**: `contract expected_collateral checklist CHANGELOG regression_test`

### 2. PRE-VALIDATION: Workspace hygiene check

**Trigger**: Before starting new bug fix workflow (Phase 1)
**Action**: Check for temporary files/directories from previous workflows (.claude/bug-pipeline-improvements/, .claude/feature-pipeline-improvements/, etc.). Clean up if found. Log warning if CLAUDE.md cleanup directive was violated by previous workflow.
**Query**: `CLAUDE.md cleanup temporary files .claude/`

### 3. DOCUMENTATION: Auto-verify CHANGELOG.md format

**Trigger**: During Phase 6 documentation or remediation when CHANGELOG.md is updated
**Action**: Verify CHANGELOG.md entry follows existing pattern (symptom, root cause, fix, testing, reference, date). Compare structure to most recent entry for format consistency.
**Query**: `CHANGELOG.md format validation pattern matching`
